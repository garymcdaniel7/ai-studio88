import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // React 19 compiler diagnostics are useful, but this legacy app has
      // many pre-existing violations. Keep them visible without blocking
      // production builds while the codebase is migrated incrementally.
      "react-hooks/immutability": "warn",
      "react-hooks/purity": "warn",
      "react-hooks/refs": "warn",
      "react-hooks/set-state-in-effect": "warn",
      "react/no-unescaped-entities": "warn",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
    // Generated Playwright/visual-audit artifacts are not source code.
    "playwright-report/**",
    "test-results/**",
    "redteam-audit/**",
    "visual-audit/**",
  ]),
]);

export default eslintConfig;
