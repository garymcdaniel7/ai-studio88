{
  "version": "v1",
  "hooks": [
    {
      "name": "Test changed components after push",
      "trigger": "PostToolUse",
      "description": "After a git push, identify which frontend pages were modified in the commit and run only those specific tests.",
      "matcher": "execute_bash",
      "action": {
        "type": "agent",
        "prompt": "A git push just happened. Check which files changed in the last commit with `git diff --name-only HEAD~1`. If any frontend page components (frontend/src/app/*/page.tsx) or test files (frontend/e2e/*.spec.ts) were modified, run ONLY the corresponding Playwright tests for those pages. Map: /brain→brain.spec.ts, /create→create.spec.ts, /talent→talent.spec.ts, /assets→assets.spec.ts, /models→models.spec.ts, /admin→admin.spec.ts, /training→training.spec.ts, /settings→settings.spec.ts, /production→production.spec.ts, /publish→publish.spec.ts, /analytics→analytics.spec.ts, /editor→editor.spec.ts, /workflows→workflows.spec.ts, /admin/fleet→fleet.spec.ts. Run: `cd frontend && npx playwright test e2e/{matched}.spec.ts --project=desktop --workers=1`. If no frontend files changed, skip testing. Update .kiro/steering/uat-system.md only if tests were run."
      }
    }
  ]
}
